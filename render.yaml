services:
  - type: web
    name: link-guard-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python link_guard_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: OPENAI_API_KEY
        sync: false
